@default:
    just --choose

system-update:
    if [ ! "$(command -v nala)" ]; then
        sudo apt update
        sudo apt upgrade
    else
        sudo nala update
        sudo nala upgrade
    fi

home-update:
    nh home switch "github:ModestMeowth/dotfiles" -- --refresh

update: system-update home-update
